services:
  db:
    image: postgres:18
    container_name: yarn-stash-db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - yarn_stash_data:/var/lib/postgresql
volumes:
  yarn_stash_data:
